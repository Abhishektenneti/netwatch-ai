# 2026-04-25

## Processor service — wrap-up

Finished the `processor` Java Spring Boot service (all components were stubbed; this session wired them together and added production-grade pieces).

- **Async training** — moved Isolation Forest training off the Kafka consumer thread onto a dedicated `if-trainer` executor pool. A `trainingInFlight` guard prevents duplicate concurrent jobs per device. Periodic retraining fires every `sampleSize` new events.
- **MLflow wiring** — `ModelTrackingService` (previously a dead bean) is now called after every `trainForest()`, logging `num_trees`, `sample_size`, `window_size`, and the rolling mean anomaly score since the last retrain.
- **`StatsController`** (`GET /stats`) — new endpoint: `events_consumed`, `events_dropped`, `anomalies_detected`, `anomaly_rate`, `devices_with_models`, `models_trained`.
- **DLQ** — `raw-events.DLT` topic + `DefaultErrorHandler` (3×500ms retries) so poison pills don't wedge the consumer.
- **`JacksonConfig`** — `JavaTimeModule` registered globally.
- **Actuator** — `/actuator/health`, `/actuator/info`, `/actuator/metrics` exposed.

Tests: 19/19 pass. `bootJar` builds clean.

---

## API service — fully implemented

### Config
- `NetwatchProperties` — typed config record for `netwatch.qdrant.*` and `netwatch.kafka.topics.*`.
- `QdrantConfig` — Qdrant gRPC client bean (read-only; collection creation stays in processor).
- `KafkaConfig` — topic declarations for `anomaly-events`, `explained-events`, `processed-events` + DLQ error handler.
- `JacksonConfig`, `build.gradle` — `JavaTimeModule`, actuator, explicit gRPC transitive deps.

### Data & services
- **`AlertRepository`** — bounded in-memory store (10k cap, LRU eviction). Indexes: `byEventId` (O(1)), `byDeviceId`, paginated `findAll`. `updateExplanation()` atomically replaces a record when the LLM result arrives.
- **`AlertService`** — three `@KafkaListener`s: (1) `anomaly-events` → store alert, (2) `explained-events` → attach explanation, (3) `processed-events` → per-device total event counter for accurate anomaly rate.
- **`DeviceService`** — Qdrant `RecommendPoints` finds nearest-neighbour devices by behaviour embedding; gracefully returns empty list if device has no embedding yet.

### Endpoints
| Method | Path | Description |
|---|---|---|
| GET | `/api/v1/alerts` | Paginated alert list (`?page=0&limit=50`) |
| GET | `/api/v1/alerts/{eventId}` | Single alert (404 if evicted) |
| GET | `/api/v1/alerts/device/{deviceId}` | All alerts for a device |
| GET | `/api/v1/devices/{deviceId}/stats` | `totalEvents`, `anomalyCount`, `anomalyRate` |
| GET | `/api/v1/devices/{deviceId}/similar` | Similar devices via Qdrant (`?n=5`) |

Tests: Spring context loads. `bootJar` builds clean.

---

## Explainer service — fully implemented

- **`consumer.py`** — `confluent_kafka.Consumer`, 1-second poll loop, SIGTERM → clean shutdown.
- **`ollama_client.py`** — `httpx.AsyncClient` → Ollama `/api/generate` (non-streaming). Prompt includes device ID, IPs/ports, protocol, byte counts, packets, duration, anomaly score (3dp), flags.
- **`main.py`** — consume → explain → produce enriched event (original fields + `explanation`) to `explained-events` topic.

Tests: 4/4 pass (prompt rendering, happy path mock, missing flags edge case, 5xx propagation).

---

## docker-compose

Added `explained-events` topic (3 partitions) to `kafka-init`.

claude --resume 38e20c84-6571-4917-8ea7-c42305a86d53 